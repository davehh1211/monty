#include "monty.h"
/**
 * o_nop - doesn't do anything (linked list)
 * @stack: the stack to do void
 * @line_number: the line of the file read
 * Return: nothing
 */
void o_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
